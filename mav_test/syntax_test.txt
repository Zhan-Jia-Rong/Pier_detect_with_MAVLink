#!/usr/bin/env python3

import sys
import cv2
import numpy as np
import time

gps_lat_aver = []  
a=0
for x in range(10):
	gps_lat_aver.append(x)
	#print(gps_lat_aver[x])
if a!=0 :
	print(0/1)
print(sum(gps_lat_aver)/len(gps_lat_aver))

gps_lat_aver.clear()